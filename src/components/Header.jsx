import { useState, useEffect, useCallback } from 'react';import { Link, useLocation, useNavigate } from 'react-router-dom';import { useCart } from '../context/CartContext';import { openCartDrawer } from '../hooks/useCartDrawer';import { IconArrowLeft, IconArrowRight, IconHome, IconSearch, IconShoppingBag, IconUser } from '../components/Icons';import './Header.css';const announcements = [  "India's Leading Gifting Solution",  "Free delivery on orders above ₹150",  "Express delivery in 3 hours for select locations"];const navLinks = [  { label: 'Shop by Occasion', href: '/collections/occasion' },  { label: 'Gifts For', href: '/collections/recipient' },  { label: 'Best Sellers', href: '/collections/best-sellers' },  { label: 'Premium', href: '/collections/premium' },  { label: 'About', href: '/about' },  { label: 'Contact', href: '/contact' },];export default function Header() {  const [announcementIndex, setAnnouncementIndex] = useState(0);  const [scrolled, setScrolled] = useState(() => window.scrollY > 0);  const [searchOpen, setSearchOpen] = useState(false);  const [searchQuery, setSearchQuery] = useState('');  const { itemCount } = useCart();  const navigate = useNavigate();  const location = useLocation();  useEffect(() => {    const interval = setInterval(() => {      setAnnouncementIndex(prev => (prev + 1) % announcements.length);    }, 4000);    return () => clearInterval(interval);  }, []);  useEffect(() => {    const onScroll = () => setScrolled(window.scrollY > 0);    window.addEventListener('scroll', onScroll);        return () => window.removeEventListener('scroll', onScroll);
  }, []);  useEffect(() => {    setSearchOpen(false);  }, [location.pathname]);  const prevAnnouncement = useCallback(() => {    setAnnouncementIndex(prev => (prev - 1 + announcements.length) % announcements.length);  }, []);  const nextAnnouncement = useCallback(() => {    setAnnouncementIndex(prev => (prev + 1) % announcements.length);  }, []);  const handleSearch = (e) => {    e.preventDefault();    if (searchQuery.trim()) {      setSearchOpen(false);      navigate(`/collections?q=${encodeURIComponent(searchQuery.trim())}`);    }  };  return (    <>      <header className={`header ${scrolled ? 'header-scrolled' : ''}`}>        <div className="announcement-bar">          <div className="announcement-inner">            <button className="announcement-arrow" onClick={prevAnnouncement} aria-label="Previous"><IconArrowLeft size={16} /></button>            <span className="announcement-text">{announcements[announcementIndex]}</span>            <button className="announcement-arrow" onClick={nextAnnouncement} aria-label="Next"><IconArrowRight size={16} /></button>          </div>        </div>        <nav className="navbar">          <div className="navbar-inner container">            <Link to="/" className="logo">Wrappd Gift</Link>            <ul className="nav-links">              {navLinks.map(link => (                <li key={link.label}>                  <Link to={link.href} className="nav-link">{link.label}</Link>                </li>              ))}            </ul>            <div className="nav-icons">              <button className="nav-icon" onClick={() => setSearchOpen(!searchOpen)} aria-label="Search">                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">                  <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>                </svg>              </button>              <button className="nav-icon cart-icon" onClick={openCartDrawer} aria-label="Cart">                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">                  <circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2 2-1.61L23 6H6"/>                </svg>                {itemCount > 0 && <span className="cart-count">{itemCount}</span>}              </button>            </div>          </div>        </nav>      </header>      <div className="mobile-navbar">        <nav className="bottom-nav">          <Link to="/" className={`nav-item ${location.pathname === '/' ? 'active' : ''}`} aria-label="Home">            <IconHome size={20} />            <span>Home</span>          </Link>          <button className={`nav-item ${searchOpen ? 'active' : ''}`} onClick={() => setSearchOpen(!searchOpen)} aria-label="Search">            <IconSearch size={20} />            <span>Search</span>          </button>          <Link to="/cart" className={`nav-item ${location.pathname === '/cart' ? 'active' : ''}`} aria-label="Cart">            <IconShoppingBag size={20} />            <span>Cart</span>          </Link>          <Link to="/wishlist" className={`nav-item ${location.pathname === '/wishlist' ? 'active' : ''}`} aria-label="Wishlist">            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>            <span>Wishlist</span>          </Link>          <Link to="/profile" className={`nav-item ${location.pathname === "/profile" ? "active" : ""}`} aria-label="Profile">            <IconUser size={20} />            <span>Profile</span>          </Link>        </nav>      </div>      {searchOpen && (        <div className="search-overlay">          <div className="search-overlay-inner container">            <form onSubmit={handleSearch} className="search-form">              <IconSearch size={20} />              <input                type="text"                className="search-input"                value={searchQuery}                onChange={e => setSearchQuery(e.target.value)}                placeholder="Search products..."                autoFocus              />              <button type="button" className="search-close" onClick={() => setSearchOpen(false)}>Cancel</button>            </form>          </div>        </div>      )}    </>  );}